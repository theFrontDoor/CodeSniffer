<?xml version="1.0"?>
<ruleset name="TFD">
    <description>The TFD coding standard.</description>

    <!-- 2. General -->

    <rule ref="TFD.PHP.DisallowShortOpenTag.Found"/>

    <rule ref="Internal.NoCodeFound">
        <severity>0</severity>
    </rule>

    <rule ref="Generic.Functions.OpeningFunctionBraceKernighanRitchie"/>

    <rule ref="Generic.Commenting.Todo"/>

    <rule ref="Squiz.Scope.StaticThisUsage"/>

    <!-- 2.1 Basic Coding Standard -->

    <rule ref="TFD.Strings.DoubleQuoteUsage.ContainsVar">
        <severity>0</severity>
    </rule>

    <!-- no spaces in cast parentheses -->
    <rule ref="Squiz.WhiteSpace.CastSpacing"/>

    <!-- space after cast -->
    <rule ref="Generic.Formatting.SpaceAfterCast"/>

    <!-- 2.2 Files -->

    <!-- The closing ?> tag MUST be omitted from files containing only PHP. -->
    <rule ref="PSR2.Files.ClosingTag"/>
    <rule ref="PSR2.Files.EndFileNewline"/>

    <!-- 2.3 Lines -->

    <!-- There MUST NOT be trailing whitespace at the end of non-blank lines. -->
    <rule ref="Squiz.WhiteSpace.SuperfluousWhitespace">
        <properties>
            <property name="ignoreBlankLines" value="true"/>
        </properties>
    </rule>
    <rule ref="Squiz.WhiteSpace.SuperfluousWhitespace.StartFile">
        <severity>0</severity>
    </rule>
    <rule ref="Squiz.WhiteSpace.SuperfluousWhitespace.EndFile">
        <severity>0</severity>
    </rule>
    <rule ref="Squiz.WhiteSpace.SuperfluousWhitespace.EmptyLines">
        <severity>0</severity>
    </rule>

    <!-- There MUST NOT be more than one statement per line. -->
    <rule ref="Generic.Formatting.DisallowMultipleStatements">
        <severity>0</severity>
    </rule>

    <!-- 2.4 Indenting -->

    <!-- Code MUST use an indent of 4 spaces, and MUST NOT use tabs for indenting. -->
    <rule ref="Generic.WhiteSpace.ScopeIndent">
        <properties>
            <property name="ignoreIndentationTokens" type="array" value="T_COMMENT,T_DOC_COMMENT"/>
        </properties>
    </rule>
    <rule ref="Generic.WhiteSpace.DisallowTabIndent"/>

    <!-- 2.5 Keywords and True/False/Null -->

    <rule ref="Generic.NamingConventions.UpperCaseConstantName"/>

    <!-- PHP keywords MUST be in lower case. -->
    <rule ref="Generic.PHP.LowerCaseKeyword"/>

    <!-- The PHP constants TRUE, FALSE, and NULL MUST be in upper case. -->
    <rule ref="Generic.PHP.UpperCaseConstant"/>

    <!-- 3. Namespace and Use Declarations -->

    <!-- 4. Classes, Properties, and Methods -->

    <rule ref="Generic.Classes.DuplicateClassName"/>
    <rule ref="TFD.Classes.ClassBraces"/>
    <rule ref="PEAR.NamingConventions.ValidClassName"/>

    <!-- 4.1. Extends and Implements -->

    <!-- 4.2. Properties -->

    <rule ref="TFD.NamingConventions.PropertyName"/>

    <!-- 4.3 Methods -->

    <rule ref="TFD.NamingConventions.MethodName"/>

    <rule ref="Generic.CodeAnalysis.UselessOverridingMethod"/>

    <!-- Visibility MUST be declared on all methods. -->
    <rule ref="Squiz.Scope.MethodScope"/>
    <rule ref="Squiz.WhiteSpace.ScopeKeywordSpacing"/>

    <!-- Method names MUST NOT be declared with a space after the method name. The opening brace MUST go on its own line, and the closing brace MUST go on the next line following the body. There MUST NOT be a space after the opening parenthesis, and there MUST NOT be a space before the closing parenthesis. -->
    <rule ref="Squiz.Functions.FunctionDeclaration"/>
    <rule ref="Squiz.Functions.LowercaseFunctionKeywords"/>

    <!-- 4.4 Method Arguments -->

    <rule ref="Generic.CodeAnalysis.UnusedFunctionParameter"/>

    <!-- In the argument list, there MUST NOT be a space before each comma, and there MUST be one space after each comma.
    <rule ref="Squiz.Functions.FunctionDeclarationArgumentSpacing">
        <properties>
            <property name="equalsSpacing" value="1"/>
        </properties>
    </rule>
    <rule ref="Squiz.Functions.FunctionDeclarationArgumentSpacing.SpacingAfterHint">
        <severity>0</severity>
    </rule>
    -->

    <!-- Method arguments with default values MUST go at the end of the argument list. -->
    <rule ref="PEAR.Functions.ValidDefaultValue"/>

    <!-- 4.5 abstract, final, and static -->

    <!-- 4.6 Method and Function Calls -->

    <!-- 5. Control Structures -->

    <!-- The general style rules for control structures are as follows:
    There MUST be one space after the control structure keyword
    There MUST NOT be a space after the opening parenthesis
    There MUST NOT be a space before the closing parenthesis
    There MUST be one space between the closing parenthesis and the opening brace
    The structure body MUST be indented once
    The closing brace MUST be on the next line after the body
    <rule ref="Squiz.ControlStructures.ControlSignature">
        <properties>
            <property name="ignoreComments" value="true"/>
        </properties>
    </rule>
    -->
    <rule ref="Squiz.WhiteSpace.ScopeClosingBrace"/>
    <!--<rule ref="Squiz.ControlStructures.ForEachLoopDeclaration"/>-->
    <!--<rule ref="Squiz.ControlStructures.ForLoopDeclaration"/>-->
    <rule ref="Squiz.ControlStructures.LowercaseDeclaration"/>
    <rule ref="TFD.ControlStructures.ControlStructureSpacing"/>

    <!-- The body of each structure MUST be enclosed by braces. This standardizes how the structures look, and reduces the likelihood of introducing errors as new lines get added to the body. -->
    <rule ref="TFD.ControlStructures.InlineControlStructure"/>

    <!-- 5.1. if, elseif, else -->

    <!-- The keyword elseif SHOULD be used instead of else if so that all control keywords look like single words. -->
    <rule ref="PSR2.ControlStructures.ElseIfDeclaration"/>

    <!-- 5.2. switch, case -->

    <!-- The case statement MUST be indented once from switch, and the break keyword (or other terminating keyword) MUST be indented at the same level as the case. There MUST be a comment such as // no break when fall-through is intentional in a non-empty case body. -->
    <rule ref="TFD.ControlStructures.SwitchDeclaration"/>

    <!-- 6. Closures -->

    <!-- Closures MUST be declared with a space after the function keyword, and a space before and after the use keyword.
    The opening brace MUST go on the same line, and the closing brace MUST go on the next line following the body.
    There MUST NOT be a space after the opening parenthesis of the argument list or variable list, and there MUST NOT be a space before the closing parenthesis of the argument list or variable list.
    In the argument list and variable list, there MUST NOT be a space before each comma, and there MUST be one space after each comma.
    Closure arguments with default values MUST go at the end of the argument list.
    Argument lists and variable lists MAY be split across multiple lines, where each subsequent line is indented once. When doing so, the first item in the list MUST be on the next line, and there MUST be only one argument or variable per line.
    When the ending list (whether or arguments or variables) is split across multiple lines, the closing parenthesis and opening brace MUST be placed together on their own line with one space between them. -->
    <!-- checked in Squiz.Functions.MultiLineFunctionDeclaration -->
</ruleset>
